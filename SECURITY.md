# 安全说明

请通过 GitHub Security Advisories 私下报告安全问题，不要在公开 Issue 中提交真实模型内容、私人路径、访问令牌或其他敏感数据。

本项目的设计边界是：

- 不主动联网；
- 不修改原始模型文件；
- 只读扫描模型元数据，并在明确的定向路径上读取有限浮点窗口；
- 未知结构、身份不一致、越界或摘要不一致时严格停止或回退；
- 不把结构命中、抽样相似度或 I/O 字节减少解释为语义准确率或完整推理加速。

公开发布的 Windows 可执行文件当前可能没有代码签名。下载二进制后应先核对 Release 页面公布的 SHA-256；对供应链有更高要求时，请直接从本仓库源码构建。

